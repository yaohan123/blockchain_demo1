server:
  # 服务http端口
  port: 8090
  # tomcat配置
  tomcat:
    # 最大线程数
    max-threads: 6
    # 最小线程数
    min-spare-threads: 3
    # 队列长度
    accept-count: 10
    # 最大链接数
    max-connections: 1000


spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: yaohan9414
    url: jdbc:mysql://127.0.0.1/LikeAndShare?characterEncoding=utf-8&userSSL=false

  jpa:
    show-sql: true

  mybatis:
    type-aliases-package: com.example.domain
    mapper-locations: classpath*:com/example/mapper/*.xml
    configuration:
      map-underscore-to-camel-case: true

  redis:
    database: 0
    host: localhost
    port: 6379
    password:
    jedis:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0
    timeout: 0